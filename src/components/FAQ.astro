---
const { items = [], heading = "FAQs" } = Astro.props;
---
<section class="mt-10">
  <h2 class="text-2xl font-bold">{heading}</h2>
  <div class="mt-4 space-y-3">
    {items.map((q) => (
      <details class="rounded-xl bg-[var(--brand-surface)] p-4 ring-1 ring-slate-200">
        <summary class="cursor-pointer font-semibold">{q.q}</summary>
        <p class="mt-2 text-slate-700">{q.a}</p>
      </details>
    ))}
  </div>
</section>
