---
import Layout from "../layouts/WMBase.astro"; import Breadcrumbs from "../components/Breadcrumbs.astro"; import Picture from "../components/Picture.astro"; import QuoteInline from "../components/QuoteInline.astro"; import FAQ from "../components/FAQ.astro";
const title = "Security Stays Wellington | Window Restrictors"; const description = "Window security stays/restrictors supplied and fitted across Wellington, Porirua and the Hutt to improve safety and security while keeping ventilation."; const canonical = "https://windowmaintenance.co.nz/security/";
---
<Layout title={title} description={description} canonical={canonical}>
  <Breadcrumbs />
  <main class="mx-auto max-w-4xl px-6 py-12">
    <h1 class="text-3xl font-extrabold">Security Stays</h1>
    <p class="mt-4 text-slate-300">We install security stays/restrictors to limit opening for child safety and ground‑floor security while keeping airflow.</p>
    <div class="mt-6 grid gap-4 sm:grid-cols-2">
      <figure class="rounded-xl bg-[var(--brand-surface)] p-4 ring-1 ring-white/10">
        <div class="ratio-4-3 card-frame"><Picture base="security-stays" alt="Security stays fitted to window" className="h-full w-full object-cover" /></div>
        <figcaption class="mt-2 text-sm text-slate-400">Security stays / restrictors</figcaption>
      </figure>
      <figure class="rounded-xl bg-[var(--brand-surface)] p-4 ring-1 ring-white/10">
        <div class="ratio-4-3 card-frame"><Picture base="sash-handles" alt="Handles and latches serviced" className="h-full w-full object-cover" /></div>
        <figcaption class="mt-2 text-sm text-slate-400">Handles & latches serviced</figcaption>
      </figure>
    </div>
  </main>
  <QuoteInline title="Get a quote for security stays" />
  <FAQ items={[
    { q: 'Can we still ventilate with security stays?', a: 'Yes — restrictors allow a safe opening while limiting window travel. Great for child safety and ground‑floor security.' },
    { q: 'Are they removable later?', a: 'They can be adjusted or replaced, and we match finishes to your aluminium joinery.' }
  ]} />
</Layout>
